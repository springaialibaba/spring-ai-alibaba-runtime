server:
  port: 10001

spring:
  config:
    import:
      - classpath:application-nacos.yml
      - classpath:application-new-agent.yml
      - classpath:application-runtime.yml
  application:
    name: spring-ai-alibaba-a2a-server


management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

controller:
  endpoints:
    process: '/agent'
    health: '/health'
    readiness: '/readiness'
    liveness: '/liveness'

logging:
  level:
    com:
      alibaba:
        cloud:
          ai:
            graph: DEBUG


version: v1
metadata:
  id: optional
  name: "my_assistant"
  description: "An AI assistant for various tasks"
  mode: agent # 疑惑
  tags: # 参考kagent/rowboat用于分组/治理/检索
    - domain: knowledge
    - env: prod
    - owner: saa
